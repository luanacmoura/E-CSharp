using System;

namespace varios_num
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Faça um programa que receba vários números. Baseado nos números inseridos, calcule e exiba: 
            A quantidade de números digitados;             
            A soma dos números digitados; 
            A média dos números digitados; 
            A média dos números pares; 
            A porcentagem de números impares que foram inseridos; 
            Finalize a entrada de números quando o usuário digitar o número 0 (Este deverá ser incluído nos cálculos acima).  */

            // resolve com arraylist, p.s pesquisar como usa!
            // ArrayList lista = new ArrayList();
            // lista.Add("pedro");
            // lista.Add("paulo");
            // foreach (string s in lista)
            // MessageBox.Show(s);
        }
    }
}
